# Rules that MUST not count in a heading

Intro paragraph. A client MUST send a handshake first, and it
MUST NOT send hooks before that. Inline `MUST` is a mention. <!-- MUST -->

- Item one SHOULD be short.
  It MAY continue lazily.
- Item two
  1. nested MAY
  2. nested plain

| Field | Required | Note |
|---|---|---|
| `a` | REQUIRED | escaped \| pipe |
| `b` | OPTIONAL, MAY be empty | none |

```text
Fenced MUST not count.
```

> **Chain hashing (normative).** Entries MUST chain.

**Enforcement (normative).** The framework MUST enforce it.

### 1.1 Failure (normative)

The concept is defined in [Thing](../concepts/thing.md) (normative).

---

Trailing paragraph with RECOMMENDED default and NOT RECOMMENDED form.
