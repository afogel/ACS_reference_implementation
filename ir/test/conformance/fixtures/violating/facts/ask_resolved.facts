# ask_resolved(seq, session, approver)  source: guardian-state
# The Guardian's record that an Approver, by `ask_details.approver.id`, returned the decision at
# `seq`. Guardian-to-Approver traffic is not in the Observed Agent's log.
60	session-violating	approver-1
