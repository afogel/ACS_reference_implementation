# provenance_fingerprint(session, pid, fingerprint)  source: wire
# Hash of a Provenance object's own fields, so two different objects sharing a `provenance_id` are
# visible.
session-violating	prov-user-input	fingerprint-a
session-violating	prov-user-input	fingerprint-b
session-violating	prov-agent	fingerprint-c
