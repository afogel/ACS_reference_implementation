# Fixture: conformant

One session, `s2`, that breaches nothing: handshake first, a DENY with its `reasoning`, chain heads published and signature-covered, lineage within the session with monotone trust, an Intent extension that legitimately widens `Intent.parsed`, and matching entry hashes. Both engines must derive an empty violation set. It also cites ACS-REQ-0007, ACS-REQ-0010, ACS-REQ-0011, ACS-REQ-0013 and ACS-REQ-0017 as the provisions its clean run exercises most directly.
